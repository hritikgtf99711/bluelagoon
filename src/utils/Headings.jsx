const Headings=({headings,classNames=""})=>{
return   <h2 className={`text-[var(--primary-color)] text-[36px] text-center uppercase font-[600] ${classNames}`}>{headings}</h2>
}
export default Headings